name: twinmart_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # Firebase Core and Services
  firebase_core: ^3.2.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.1.0
  firebase_storage: ^12.0.0   # ✅ ADDED (for product images)

  # Utility and Logic Packages
  intl: ^0.19.0
  provider: ^6.1.1
  
  # Feature Packages
  mobile_scanner: ^5.2.1
  image_picker: ^1.0.7        # ✅ ADDED (for picking images)
  qr_flutter: ^4.1.0
  fl_chart: ^1.1.1
  shimmer: ^3.0.0
  flutter_local_notifications: ^20.1.0
  firebase_messaging: ^15.2.10
  url_launcher: ^6.3.2
  csv: ^7.2.0
  path_provider: ^2.1.5
  share_plus: ^12.0.1
  pdf: ^3.11.3
  printing: ^5.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/           # ✅ ADDED (create this folder)
